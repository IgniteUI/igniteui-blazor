@page "/IgbRatingComponent"
@using IgniteUI.Blazor.Controls

<PageTitle>IgbRatingComponent</PageTitle>

<h3>IgbRatingComponent</h3>

<div class="controls">
    <div class="control">
        <label>Max: @Max</label>
        <div class="control-box">
            <IgbSlider class="max-slider" @bind-Value="Max" Min="3" Max="10" Step="1" />
        </div>
    </div>

    <div class="control">
        <label>Options</label>
        <div class="control-box switches">
            <IgbSwitch @bind-Checked="HalfSteps">Half steps</IgbSwitch>
            <IgbSwitch @bind-Checked="HoverPreview">Hover preview</IgbSwitch>
            <IgbSwitch @bind-Checked="AllowReset">Allow reset</IgbSwitch>
            <IgbSwitch @bind-Checked="ReadOnly">Read-only</IgbSwitch>
            <IgbSwitch @bind-Checked="Disabled">Disabled</IgbSwitch>
        </div>
    </div>
</div>

<div class="component-host">
    <IgbRating @bind-Value="Value"
               Label="Rate your experience"
               Max="Max"
               Step="Step"
               HoverPreview="HoverPreview"
               AllowReset="AllowReset"
               ReadOnly="ReadOnly"
               Disabled="Disabled" />
</div>

<div class="state">Value: @Value / @Max</div>

@code {
    /*
     * Ignite UI for Blazor - open-source (MIT) component from IgniteUI.Blazor.Lite.
     * An item template can't modify project files, so do this once per project:
     *
     * 1. Install:   dotnet add package IgniteUI.Blazor.Lite
     * 2. Register (Program.cs):   builder.Services.AddIgniteUIBlazor();
     * 3. _Imports.razor:          @using IgniteUI.Blazor.Controls
     *    (optional here — this file already declares its own @using;
     *    add it to _Imports.razor to share it project-wide)
     * 4. Host page theme (index.html / App.razor / _Host.cshtml):
     *    <link href="_content/IgniteUI.Blazor/themes/light/bootstrap.css" rel="stylesheet" />
     * 5. Blazor Web App (.NET 8+): add an interactive render mode to this page,
     *    e.g. @rendermode InteractiveServer (or InteractiveWebAssembly / InteractiveAuto).
     *
     * Do not mix IgniteUI.Blazor.Lite with the licensed IgniteUI.Blazor package.
     */

    private double Value { get; set; } = 3;
    private double Max { get; set; } = 5;

    private bool HalfSteps { get; set; }
    private double Step => HalfSteps ? 0.5 : 1;

    private bool HoverPreview { get; set; } = true;
    private bool AllowReset { get; set; } = true;
    private bool ReadOnly { get; set; }
    private bool Disabled { get; set; }
}
