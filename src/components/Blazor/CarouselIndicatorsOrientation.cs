namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// Placement of the slide indicators (dots) of an <see cref="IgbCarousel"/>.
    /// </summary>
    public enum CarouselIndicatorsOrientation
    {
        /// <summary>After the slides: below them in a horizontal carousel, at the inline end (right in left-to-right layouts) in a vertical one.</summary>
        End,
        /// <summary>Before the slides: above them in a horizontal carousel, at the inline start (left in left-to-right layouts) in a vertical one.</summary>
        Start

    }
}
