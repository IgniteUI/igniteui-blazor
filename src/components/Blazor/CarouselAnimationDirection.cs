namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// Direction of the slide transition when an <see cref="IgbCarousel"/> selects a slide programmatically.
    /// </summary>
    public enum CarouselAnimationDirection
    {
        /// <summary>Animate as if moving forward to a later slide.</summary>
        Next,
        /// <summary>Animate as if moving back to an earlier slide.</summary>
        Prev

    }
}
