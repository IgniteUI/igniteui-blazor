namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// How an <see cref="IgbColorPicker"/> presents the anchor that opens it.
    /// </summary>
    public enum ColorPickerMode
    {
        /// <summary>The anchor is a trigger button.</summary>
        Default,
        /// <summary>The anchor is an editable text field with a color swatch prefix that also opens the picker.</summary>
        Input

    }
}
