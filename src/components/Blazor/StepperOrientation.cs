namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// Arrangement of the steps of an <see cref="IgbStepper"/>.
    /// </summary>
    public enum StepperOrientation
    {
        /// <summary>Steps in a row. The active step's content is shown below the step headers.</summary>
        Horizontal,
        /// <summary>Steps in a column. Each step's content is shown under its own header.</summary>
        Vertical

    }
}
