namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// Identifies one of the two panes of an <see cref="IgbSplitter"/>.
    /// </summary>
    public enum PanePosition
    {
        /// <summary>The first pane: top in a vertical splitter, inline start (left in left-to-right layouts) in a horizontal one.</summary>
        Start,
        /// <summary>The second pane: bottom in a vertical splitter, inline end (right in left-to-right layouts) in a horizontal one.</summary>
        End

    }
}
