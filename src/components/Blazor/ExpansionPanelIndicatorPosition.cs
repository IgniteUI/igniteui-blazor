namespace IgniteUI.Blazor.Controls
{
    /// <summary>
    /// Placement of the expand/collapse indicator in an <see cref="IgbExpansionPanel"/> header.
    /// </summary>
    public enum ExpansionPanelIndicatorPosition
    {
        /// <summary>Before the header content.</summary>
        Start,
        /// <summary>After the header content.</summary>
        End,
        /// <summary>No indicator is rendered.</summary>
        None

    }
}
